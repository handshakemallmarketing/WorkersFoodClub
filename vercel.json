{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm add -w --lockfile=false @neondatabase/serverless@1.1.0 && pnpm run build",
  "rewrites": [
    {
      "source": "/api/build-info",
      "destination": "/api/db-health?probe=build-info"
    }
  ]
}
