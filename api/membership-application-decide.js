import { randomBytes, randomUUID } from 'node:crypto';
import { verifyProductionOidcRequest } from '../lib/production-oidc-auth.js';
import { requireProductionApplicationAccess } from '../lib/production-access-policy.js';
import { isFreshStepUp, verifyEmployeeSessionTokenShape, extractEmployeeSessionToken } from '../lib/employee-session.js';
import { tierOf } from '../dist/packages/authority/src/hierarchy.js';
import { ensureAnnualMembershipInvoice } from '../lib/membership-annual-invoice.js';
const newPrimaryMemberId=()=>`WFC-P-${randomBytes(6).toString('hex').toUpperCase()}`;
/** Admin/Owner review. Applicant enrollment is unauthenticated; reviewer authentication remains mandatory. */
export default async function handler(req,res,options={}){
 if(req.method!=='POST'){res.setHeader('Allow','POST');return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});}
 const env=options.env||process.env,access=requireProductionApplicationAccess(env);if(!access.ok)return res.status(access.status).json({ok:false,error:access.error});
 const verified=await verifyProductionOidcRequest(req,undefined,undefined,options.production||{});if(!verified.ok)return res.status(verified.status).json({ok:false,error:verified.error});
 if(!isFreshStepUp(verified.principal.issuedAt,options.now??Date.now()))return res.status(401).json({ok:false,error:'MEMBERSHIP_DECIDE_STEP_UP_NOT_FRESH'});
 const applicationId=req.body?.applicationId,decision=req.body?.decision;if(typeof applicationId!=='string'||!applicationId)return res.status(400).json({ok:false,error:'APPLICATION_ID_REQUIRED'});if(!['APPROVE','REJECT'].includes(decision))return res.status(400).json({ok:false,error:'DECISION_MUST_BE_APPROVE_OR_REJECT'});
 const secret=options.employeeSessionSecret||env.EMPLOYEE_SESSION_SECRET;if(typeof secret!=='string'||secret.length<32)return res.status(503).json({ok:false,error:'EMPLOYEE_SESSION_NOT_CONFIGURED'});const sessionId=verifyEmployeeSessionTokenShape(secret,options.employeeSessionToken??extractEmployeeSessionToken(req));if(!sessionId)return res.status(401).json({ok:false,error:'EMPLOYEE_SESSION_REQUIRED'});
 const connectionString=options.databaseUrl||env.DATABASE_URL;if(!connectionString&&!options.sql)return res.status(503).json({ok:false,error:'DATABASE_URL_MISSING'});
 try{let sql=options.sql;if(!sql){const {neon}=await import('@neondatabase/serverless');sql=neon(connectionString,{fetchOptions:{signal:AbortSignal.timeout(5000)}});}
  const bindings=await sql`SELECT participant_id,state FROM application_identity_binding WHERE issuer=${verified.principal.issuer} AND subject=${verified.principal.subject} LIMIT 2`;if(bindings.length!==1)return res.status(403).json({ok:false,error:'APPLICATION_IDENTITY_NOT_BOUND'});if(String(bindings[0].state)!=='ACTIVE')return res.status(403).json({ok:false,error:'APPLICATION_PRINCIPAL_DISABLED'});const callerId=String(bindings[0].participant_id);
  const sessions=await sql`SELECT session_id FROM employee_session WHERE session_id=${sessionId} AND participant_id=${callerId} AND expires_at>now() AND revoked_at IS NULL LIMIT 1`;if(sessions.length!==1)return res.status(401).json({ok:false,error:'EMPLOYEE_SESSION_INVALID'});
  const grants=await sql`SELECT actions FROM application_authority_grant WHERE actor_id=${callerId} AND valid_from<=now() AND (valid_until IS NULL OR valid_until>=now()) AND (revoked_at IS NULL OR revoked_at>now())`;if(tierOf(grants.flatMap(g=>Array.isArray(g.actions)?g.actions.map(String):[]))==='OPERATOR')return res.status(403).json({ok:false,error:'MEMBERSHIP_DECISION_REQUIRES_OWNER_OR_ADMIN'});
  const apps=await sql`SELECT application_id,state,full_name,email,phone,applicant_reference FROM membership_application WHERE application_id=${applicationId} LIMIT 1`;if(apps.length!==1)return res.status(404).json({ok:false,error:'APPLICATION_NOT_FOUND'});if(String(apps[0].state)!=='SUBMITTED')return res.status(409).json({ok:false,error:'APPLICATION_NOT_DECIDABLE'});
  const now=options.now??Date.now(),nowIso=new Date(now).toISOString();
  if(decision==='REJECT'){const rows=await sql`UPDATE membership_application SET state='REJECTED',decided_at=${nowIso},decided_by=${callerId} WHERE application_id=${applicationId} AND state='SUBMITTED' RETURNING application_id`;if(rows.length!==1)return res.status(409).json({ok:false,error:'APPLICATION_ALREADY_DECIDED'});res.setHeader('Cache-Control','no-store');return res.status(200).json({ok:true,applicationId,decision:'REJECT'});}
  const approved=await sql`UPDATE membership_application SET state='APPROVED',decided_at=${nowIso},decided_by=${callerId} WHERE application_id=${applicationId} AND state='SUBMITTED' RETURNING application_id`;if(approved.length!==1)return res.status(409).json({ok:false,error:'APPLICATION_ALREADY_DECIDED'});
  const participantId=`participant:${randomUUID()}`,membershipId=`membership:${randomUUID()}`,publicMemberId=newPrimaryMemberId();
  try{
   await sql`INSERT INTO application_participant(participant_id,kind,state) VALUES (${participantId},'PERSON','ACTIVE')`;
   await sql`INSERT INTO application_membership(membership_id,participant_id,state,member_type,standing,established_at,eligibility_policy_version,eligibility_evidence_ids,public_member_id) VALUES (${membershipId},${participantId},'INACTIVE','PRIMARY','INITIAL_FEE_DUE',${nowIso},'guest-membership-application-v5',${[applicationId]},${publicMemberId})`;
   const linked=await sql`UPDATE membership_application SET resulting_membership_id=${membershipId} WHERE application_id=${applicationId} AND state='APPROVED' AND decided_by=${callerId} AND resulting_membership_id IS NULL RETURNING application_id`;if(linked.length!==1)throw Object.assign(new Error('MEMBERSHIP_APPLICATION_LINK_FAILED'),{code:'MEMBERSHIP_APPLICATION_LINK_FAILED'});
   try{const invoice=await ensureAnnualMembershipInvoice({sql,membershipId,publicMemberId,now,env,amountMinor:options.annualFeeMinor});res.setHeader('Cache-Control','no-store');return res.status(200).json({ok:true,applicationId,decision:'APPROVE',participantId,membershipId,publicMemberId,membershipState:'INACTIVE',standing:'INITIAL_FEE_DUE',invoiceId:invoice.invoiceId,dueAt:invoice.dueAt,subscriptionInvoicePending:false,loginIdentityBound:false});}
   catch(invoiceError){const billingError=String(invoiceError?.code||'ANNUAL_SUBSCRIPTION_INVOICE_FAILED').replace(/[^A-Z0-9_]/gi,'_').slice(0,80);console.error('Approved membership created but annual invoice pending',{membershipId,publicMemberId,code:invoiceError?.code,message:invoiceError?.message});res.setHeader('Cache-Control','no-store');return res.status(200).json({ok:true,applicationId,decision:'APPROVE',participantId,membershipId,publicMemberId,membershipState:'INACTIVE',standing:'INITIAL_FEE_DUE',subscriptionInvoicePending:true,billingStatus:'PENDING_RETRY',billingError,loginIdentityBound:false});}
  }catch(e){await sql`UPDATE application_membership SET state='ENDED',standing='ENDED',ended_at=${nowIso} WHERE membership_id=${membershipId} AND state='INACTIVE'`;await sql`UPDATE membership_application SET state='SUBMITTED',decided_at=NULL,decided_by=NULL,resulting_membership_id=NULL WHERE application_id=${applicationId} AND state='APPROVED' AND decided_by=${callerId} AND resulting_membership_id IS NULL`;throw e;}
 }catch(error){console.error('Membership application decision failed',{name:error?.name,code:error?.code,message:error?.message});return res.status(503).json({ok:false,error:'MEMBERSHIP_DECIDE_FAILED'});}
}
