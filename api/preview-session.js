import { mintPreviewApiToken, isPreviewLikeEnvironment } from '../lib/preview-api-auth.js';
const PREVIEW_MEMBER_ACTOR_ID='preview:member:001';const TTL_SECONDS=600;
const MEMBER_SCOPES=['member:purchase.commit','member:payment.execute','member:fulfillment.accept','member:orders.read','member:notifications.read','member:engagement.survey','member:payroll.enroll','member:credit.request'];
export default async function handler(req,res){if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});}if(!isPreviewLikeEnvironment())return res.status(403).json({ok:false,error:'PREVIEW_SESSION_PREVIEW_ONLY'});const secret=process.env.PREVIEW_API_AUTH_SECRET;if(typeof secret!=='string'||secret.length<32)return res.status(503).json({ok:false,error:'PREVIEW_AUTH_NOT_CONFIGURED'});const issuedAt=Math.floor(Date.now()/1000),expiresAt=issuedAt+TTL_SECONDS;const memberToken=mintPreviewApiToken({secret,subject:'preview-member-session',actorId:PREVIEW_MEMBER_ACTOR_ID,scopes:MEMBER_SCOPES,issuedAt,expiresAt});res.setHeader('Cache-Control','no-store');return res.status(200).json({ok:true,memberToken,expiresAt});}
