# pilot-checkout

SW1 application service composing authenticated active-member resolution, governed catalog views, non-binding carts and the SW0 demand commitment ledger. A cart never creates an Obligation. Checkout requires a verified accepted command/event and revalidates membership and offer executability.
