import test from 'node:test';import assert from 'node:assert/strict';import applyHandler from '../../api/membership-apply.js';
function response(){const result={statusCode:null,body:null,headers:{}};return{result,setHeader(n,v){result.headers[String(n).toLowerCase()]=v;return this},status(c){result.statusCode=c;return this},json(b){result.body=b;return this}}}function request(method,body={},headers={}){return{method,headers,body}}
function autoSql({invoice=true}={}){const calls=[];const sql=async(strings,...values)=>{const text=strings.join('?');calls.push({text,values});if(text.includes('INSERT INTO membership_application')){const membershipId=values.find(v=>typeof v==='string'&&v.startsWith('membership:'));return[{application_id:'application:test',resulting_membership_id:membershipId}]};if(text.includes('WITH member AS')){if(!invoice)throw Object.assign(new Error('fee missing'),{code:'ANNUAL_MEMBERSHIP_FEE_NOT_CONFIGURED'});const membershipId=values.find(v=>typeof v==='string'&&v.startsWith('membership:'));return[{invoice_id:'subscription-invoice:1',membership_id:membershipId,subscription_year:2026,amount_minor:12000,currency:'GHS',state:'OPEN',due_at:'2026-10-05T00:00:00.000Z',existing:false}]};throw new Error('UNEXPECTED_QUERY')};sql.calls=calls;return sql}function manualSql(){const calls=[];const sql=async(strings,...values)=>{calls.push({text:strings.join('?'),values});return[]};sql.calls=calls;return sql}const base=(sql,mode='AUTO')=>({env:{VERCEL_ENV:'preview',MEMBERSHIP_APPLICATION_REVIEW_MODE:mode},sql,annualFeeMinor:12000,now:Date.parse('2026-09-21T00:00:00Z')});const valid={fullName:'Ama Mensah',governmentEmployer:'Ghana Health Service',email:'AMA@example.com',contactNote:' hello '};
test('rejects non-POST',async()=>{const r=response();await applyHandler(request('GET'),r,{});assert.equal(r.result.statusCode,405)});
test('AUTO creates numbered inactive member then annual invoice',async()=>{const sql=autoSql(),r=response();await applyHandler(request('POST',valid),r,base(sql));assert.equal(r.result.statusCode,201);assert.equal(r.result.body.membershipProvisioned,true);assert.match(r.result.body.publicMemberId,/^WFC-P-[0-9A-F]{12}$/);assert.equal(r.result.body.subscriptionInvoicePending,false);assert.equal(r.result.body.invoiceId,'subscription-invoice:1');assert.equal(r.result.body.invoiceAmountMinor,12000);assert.equal(r.result.body.loginIdentityBound,false);assert.match(sql.calls[0].text,/INSERT INTO application_membership/);assert.doesNotMatch(sql.calls[0].text,/membership_subscription_invoice/);assert.match(sql.calls[1].text,/membership_subscription_invoice/)});
test('billing failure preserves successful membership and reports pending retry',async()=>{const sql=autoSql({invoice:false}),r=response();await applyHandler(request('POST',valid),r,{...base(sql),annualFeeMinor:undefined,env:{VERCEL_ENV:'preview'}});assert.equal(r.result.statusCode,201);assert.equal(r.result.body.membershipProvisioned,true);assert.equal(r.result.body.subscriptionInvoicePending,true);assert.equal(r.result.body.billingStatus,'PENDING_RETRY');assert.equal(r.result.body.billingError,'ANNUAL_MEMBERSHIP_FEE_NOT_CONFIGURED')});
test('arbitrary Authorization header has no enrollment authority',async()=>{const r=response();await applyHandler(request('POST',valid,{authorization:'Bearer arbitrary'}),r,base(autoSql()));assert.equal(r.result.statusCode,201)});test('MANUAL review remains optional governed path',async()=>{const sql=manualSql(),r=response();await applyHandler(request('POST',valid),r,base(sql,'MANUAL'));assert.equal(r.result.statusCode,201);assert.equal(r.result.body.reviewRequired,true);assert.equal(r.result.body.membershipProvisioned,false)});test('government employer required',async()=>{const r=response();await applyHandler(request('POST',{fullName:'Ama',email:'a@example.com'}),r,base(autoSql()));assert.equal(r.result.body.error,'GOVERNMENT_EMPLOYER_REQUIRED')});test('email or phone required',async()=>{const r=response();await applyHandler(request('POST',{fullName:'Ama',governmentEmployer:'GHS'}),r,base(autoSql()));assert.equal(r.result.body.error,'EMAIL_OR_PHONE_REQUIRED')});test('malformed email rejected',async()=>{const r=response();await applyHandler(request('POST',{fullName:'Ama',governmentEmployer:'GHS',email:'bad'}),r,base(autoSql()));assert.equal(r.result.body.error,'EMAIL_INVALID')});
