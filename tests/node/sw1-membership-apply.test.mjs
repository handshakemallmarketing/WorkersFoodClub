import test from 'node:test';import assert from 'node:assert/strict';import applyHandler from '../../api/membership-apply.js';
function response(){const result={statusCode:null,body:null,headers:{}};return{result,setHeader(n,v){result.headers[String(n).toLowerCase()]=v;return this},status(c){result.statusCode=c;return this},json(b){result.body=b;return this}}}function request(method,body={},headers={}){return{method,headers,body}}
function manualSql(){const calls=[];const sql=async(strings,...values)=>{calls.push({text:strings.join('?'),values});return[]};sql.calls=calls;return sql}const base=(sql,mode='AUTO')=>({env:{VERCEL_ENV:'preview',MEMBERSHIP_APPLICATION_REVIEW_MODE:mode},sql,now:Date.parse('2026-09-21T00:00:00Z')});const valid={fullName:'Ama Mensah',governmentEmployer:'Ghana Health Service',email:'AMA@example.com',contactNote:' hello '};
valid.phone='0240000000';
test('rejects non-POST',async()=>{const r=response();await applyHandler(request('GET'),r,{});assert.equal(r.result.statusCode,405)});
test('AUTO mode no longer provisions directly and points callers at the verify-first endpoint',async()=>{const sql=manualSql(),r=response();await applyHandler(request('POST',valid),r,base(sql));assert.equal(r.result.statusCode,410);assert.equal(r.result.body.error,'ENROLLMENT_FLOW_MOVED');assert.equal(r.result.body.useEndpoint,'/api/enrollment-verify-start');assert.equal(sql.calls.length,0)});
test('arbitrary Authorization header has no enrollment authority',async()=>{const r=response();await applyHandler(request('POST',valid,{authorization:'Bearer arbitrary'}),r,base(manualSql()));assert.equal(r.result.statusCode,410)});test('MANUAL review remains optional governed path',async()=>{const sql=manualSql(),r=response();await applyHandler(request('POST',valid),r,base(sql,'MANUAL'));assert.equal(r.result.statusCode,201);assert.equal(r.result.body.reviewRequired,true);assert.equal(r.result.body.membershipProvisioned,false)});test('government employer required',async()=>{const r=response();await applyHandler(request('POST',{fullName:'Ama',email:'a@example.com'}),r,base(manualSql()));assert.equal(r.result.body.error,'GOVERNMENT_EMPLOYER_REQUIRED')});test('email or phone required',async()=>{const r=response();await applyHandler(request('POST',{fullName:'Ama',governmentEmployer:'GHS'}),r,base(manualSql()));assert.equal(r.result.body.error,'EMAIL_OR_PHONE_REQUIRED')});test('malformed email rejected',async()=>{const r=response();await applyHandler(request('POST',{fullName:'Ama',governmentEmployer:'GHS',email:'bad'}),r,base(manualSql()));assert.equal(r.result.body.error,'EMAIL_INVALID')});
