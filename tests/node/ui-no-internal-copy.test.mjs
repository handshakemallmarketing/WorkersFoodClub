import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const html=fs.readFileSync(new URL('../../public/index.html',import.meta.url),'utf8');test('member shell omits infrastructure and release-control vocabulary',()=>{for(const term of ['Neon','RC2','Release Controls','Workforce & Authority','SuperUser'])assert.equal(html.includes(term),false,term);});
