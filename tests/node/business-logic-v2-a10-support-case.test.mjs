import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
const api=await readFile(new URL('../../api/support-case.js',import.meta.url),'utf8');const transition=await readFile(new URL('../../api/support-case-transition.js',import.meta.url),'utf8');const sql=await readFile(new URL('../../packages/durability/sql/012_wave2_support_case.sql',import.meta.url),'utf8');const bootstrap=await readFile(new URL('../../packages/durability/sql/013_membership_shopping_credit_accounting.sql',import.meta.url),'utf8');
test('UC-28 support command requires bounded operator authority',()=>{assert.match(api,/operator:support\.manage/);assert.match(api,/requireApplicationAuth/);assert.match(api,/preview-auth:\$\{principal\.subject\}/);assert.doesNotMatch(api,/req\.body\?\.authorityRef/);assert.doesNotMatch(transition,/req\.body\?\.authorityRef/);});
test('UC-28 support state cannot write canonical economic or physical truth',()=>{for(const x of ['preview_member_payment','preview_member_commitment','preview_member_credit','preview_member_refund','preview_fulfillment','canonical_event'])assert.equal(api.includes(x),false,x);});
test('UC-28 case lifecycle is versioned and actor/authority bound',()=>{assert.match(sql,/state_version bigint NOT NULL DEFAULT 1/);assert.match(sql,/created_by_actor_id text NOT NULL/);assert.match(sql,/created_by_authority_ref text NOT NULL/);assert.match(sql,/UNIQUE \(case_id, state_version\)/);});
test('UC-28 create replay is actor-bound and changed payload fails closed',()=>{assert.match(sql,/UNIQUE \(created_by_actor_id, command_idempotency_key\)/);assert.match(api,/sameCommand\(prior\[0\],command\)/);assert.match(api,/SUPPORT_CASE_REQUEST_REBOUND/);assert.match(api,/canonicalJson\(row\.evidence_refs\?\?\[\]\)===canonicalJson\(c\.evidenceRefs\)/);assert.match(api,/error\?\.code==='23505'&&sql/);});
test('UC-28 accepted inputs remain replay-stable and evidence is reference-only',()=>{assert.match(api,/PARTICIPANT_ID_INVALID/);assert.match(api,/EVIDENCE_REFS_INVALID/);assert.match(api,/Array\.isArray\(value\)&&value\.length<=32/);assert.match(transition,/Array\.isArray\(value\)&&value\.length<=32/);});
test('UC-28 create replay preserves immutable opening result',()=>{assert.match(api,/state:'OPEN',stateVersion:1/);assert.match(api,/updatedAt:String\(r\.opened_at\)/);});
test('UC-28 transition concurrent retry rechecks durable command identity',()=>{assert.match(transition,/const raced=await sql/);assert.match(transition,/sameCommand\(raced\[0\],command\)/);assert.match(transition,/error\?\.code==='23505'&&sql/);});
test('UC-28 support schema is reachable from checked durability bootstrap',()=>{assert.match(bootstrap,/\\ir 012_wave2_support_case\.sql/);});
test('UC-28 Production writes remain disabled',()=>{assert.match(api,/SUPPORT_CASE_WRITE_DISABLED_IN_PRODUCTION/);assert.match(transition,/SUPPORT_CASE_WRITE_DISABLED_IN_PRODUCTION/);});
