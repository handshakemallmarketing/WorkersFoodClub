import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const html=fs.readFileSync(new URL('../../public/index.html',import.meta.url),'utf8');test('guest shell starts public and keeps member routes hidden',()=>{assert.match(html,/id="audience-label">Guest</);assert.match(html,/data-view="orders" hidden/);assert.match(html,/data-view="notifications" hidden/);assert.match(html,/Browse Offers/);});
