import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const migration=fs.readFileSync(new URL('../../packages/durability/sql/020_household_beneficiary_activation.sql',import.meta.url),'utf8');const invite=fs.readFileSync(new URL('../../api/household-invite.js',import.meta.url),'utf8');const activate=fs.readFileSync(new URL('../../api/household-activate.js',import.meta.url),'utf8');const page=fs.readFileSync(new URL('../../public/activate.html',import.meta.url),'utf8');const status=fs.readFileSync(new URL('../../api/membership-status.js',import.meta.url),'utf8');const vercel=fs.readFileSync(new URL('../../vercel.json',import.meta.url),'utf8');
test('J14 persists only a hash of the single-use invitation token',()=>{assert.match(migration,/token_hash text/);assert.match(invite,/createHash\('sha256'\)/);assert.doesNotMatch(invite,/INSERT INTO household_beneficiary_invitation\([^)]*\btoken\b[^_]/)});test('J14 sponsor must be ACTIVE CURRENT PRIMARY and is limited to two slots',()=>{assert.match(invite,/state='ACTIVE' AND standing='CURRENT'/);assert.match(invite,/member_type/);assert.match(invite,/PRIMARY_MEMBERSHIP_REQUIRED/);assert.match(invite,/slots\.length>=2/);assert.match(invite,/count\(\*\).*<2/)});test('J14 activation is single-use and sponsor standing is rechecked',()=>{assert.match(activate,/SPONSOR_MEMBERSHIP_NOT_CURRENT/);assert.match(activate,/SET token_hash=NULL/);assert.match(activate,/HOUSEHOLD_INVITATION_ALREADY_REDEEMED/)});test('J14 activated beneficiary gets separate public Member ID and ordinary member scopes',()=>{assert.match(activate,/HOUSEHOLD_BENEFICIARY/);assert.match(activate,/publicMemberId=`WFC-H-/);assert.match(activate,/member:purchase\.commit/);assert.match(activate,/sponsoring_membership_id/)});test('J14 activation has a real bounded frontend route',()=>{assert.match(page,/\/api\/household-activate/);assert.match(page,/Google Sign-In/);assert.match(vercel,/"source": "\/activate"/)});test('membership-status uses lifecycle-v3 member_type rather than nonexistent role column',()=>{assert.match(status,/member_type,public_member_id/);assert.doesNotMatch(status,/SELECT membership_id,state,standing,role,/)});
