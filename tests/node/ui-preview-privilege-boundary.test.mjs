import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const convergence=fs.readFileSync(new URL('../../public/ui-convergence.js',import.meta.url),'utf8');test('preview convergence removes non-member role cards',()=>{assert.match(convergence,/if\(!\/Member\/\.test\(card\.textContent\|\|''\)\)card\.remove/);assert.match(convergence,/Member preview/);assert.match(convergence,/Membership enrollment is completed through Join Food Club/);});
