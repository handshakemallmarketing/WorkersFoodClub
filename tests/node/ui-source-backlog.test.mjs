import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const note=fs.readFileSync(new URL('../../docs/traceability/WAVE2-UI-SOURCE-SIMPLIFICATION-BACKLOG.md',import.meta.url),'utf8');test('legacy privileged preview and auto elevation are tracked for deletion',()=>{assert.match(note,/PREVIEW_ROLES/);assert.match(note,/automatic `elevateEmployeeSession\(\)`/);assert.match(note,/System Owner\/Admin/);});
