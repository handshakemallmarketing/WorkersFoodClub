import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const index=fs.readFileSync(new URL('../../public/index.html',import.meta.url),'utf8');const links=fs.readFileSync(new URL('../../public/member-links.js',import.meta.url),'utf8');const convergence=fs.readFileSync(new URL('../../public/ui-convergence.js',import.meta.url),'utf8');test('member destinations are contextual rather than guest navigation',()=>{assert.doesNotMatch(index,/>Membership</);assert.match(links,/memberAccessAvailable/);assert.match(links,/household\.html/);assert.match(convergence,/Employee Access/);});
