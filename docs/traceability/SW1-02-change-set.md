# SW1-02 change set

Primary executable changes are intentionally small: a new sales composition package and focused adversarial test file. The remaining files make scope, constitutional mappings, negative paths, residual risks and handoff semantics explicit so later product work cannot reinterpret the slice by convention.
