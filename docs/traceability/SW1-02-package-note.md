# `@foodclub/sales` package role

This package exists to keep pilot commerce composition out of the canonical catalog/economics kernels. It may coordinate reads and sales-window policy, but future consequential checkout must enter through application/command infrastructure rather than being added as a mutating method here.
