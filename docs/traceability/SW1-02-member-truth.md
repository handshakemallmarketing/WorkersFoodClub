# SW1-02 member-facing truth rules

Member-facing commercial claims must be reconstructible from canonical records. The displayed product derives from Specification + Listing; the offer derives from the bounded MemberOffer; the reference price derives from a governed BenchmarkValuation; the ordering period derives from SalesWindow.

The composition must fail closed rather than fill missing canonical context with UI defaults.
