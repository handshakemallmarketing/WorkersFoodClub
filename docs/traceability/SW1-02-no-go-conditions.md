# SW1-02 NO-GO conditions

Do not merge if a fabricated authority grant can configure a window; if an expired/closed commercial condition can be rendered executable; if listing/specification identity can be conflated; if a non-comparable benchmark can be presented as comparable; if the traceability head/evidence set drifts; or if the new package creates a duplicate mutable source of offer/benchmark truth.
