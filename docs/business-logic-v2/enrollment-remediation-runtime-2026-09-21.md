# Runtime certification

Preview runtime success requires the branch deployment to have `DATABASE_URL` and a non-production annual fee configured. A successful request must persist the application, participant, numbered membership, invoice, and application linkage atomically. Runtime verification must not use live-money authority.
