# Audit note

The corrected lifecycle intentionally separates identity assignment (Member Number) from economic activation (settlement). This preserves traceability of unpaid prospective members while preventing unpaid memberships from gaining active rights.
