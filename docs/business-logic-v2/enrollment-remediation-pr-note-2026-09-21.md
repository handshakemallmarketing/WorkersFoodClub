# PR note

This remediation is intentionally isolated from open PR #128 (catalog administration), because #128 does not own membership enrollment and predates the post-#127 lifecycle correction. The branches must not be merged together implicitly.
