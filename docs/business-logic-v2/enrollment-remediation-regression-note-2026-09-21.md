# Regression note

Regression tests explicitly forbid settlement from filling a missing Member Number. A missing number at settlement is now an invariant violation requiring reconciliation rather than an opportunity to mint identity late.
