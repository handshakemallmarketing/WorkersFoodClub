# Operator note

An operator reviewing an unpaid enrollment should see a stable Member Number and an OPEN annual invoice, but must not treat the record as active membership. Activation evidence comes from trusted settlement state, not from Member Number existence.
