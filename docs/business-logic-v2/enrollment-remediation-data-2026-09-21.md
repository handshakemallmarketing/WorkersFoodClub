# Enrollment data result

Before payment, the durable domain result is expected to include: membership application, person participant, PRIMARY membership with `public_member_id`, state `INACTIVE`, standing `INITIAL_FEE_DUE`, and annual subscription invoice state `OPEN` linked by `membership_id`.
