# Acceptance criteria

A successful AUTO enrollment response must contain a server-issued Member Number, an INACTIVE membership state, INITIAL_FEE_DUE standing, and an OPEN annual invoice identifier. No identity binding is created. Settlement must require the pre-existing Member Number and preserve it while transitioning membership to ACTIVE.
