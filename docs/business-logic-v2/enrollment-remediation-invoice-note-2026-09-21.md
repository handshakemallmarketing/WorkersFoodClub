# Invoice linkage

The annual subscription invoice remains keyed to `membership_id`; because that membership now already owns its immutable `public_member_id`, the invoice is effectively against the numbered membership without duplicating the Member Number into invoice storage.
