# UI contract

After successful automatic enrollment the UI displays the Member Number and invoice identifier and states that membership remains inactive until payment is confirmed. It must not tell the prospect that payment is what issues the Member Number.
