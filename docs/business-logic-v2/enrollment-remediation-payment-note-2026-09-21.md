# Payment note

The invoice is identified by `invoiceId` and linked to the membership containing `public_member_id`. Future payment UI should present both safely for the member, while provider settlement remains evidence-driven and server-authorized.
